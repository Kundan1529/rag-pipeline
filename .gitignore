# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables / secrets (never commit)
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store

# --- Python backend ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# --- Runtime artifacts / generated data ---
data/traces.jsonl
data/eval_results.json

# --- User-uploaded corpus (large / possibly copyrighted PDFs) ---
# The markdown demo corpus (data/sops, data/manuals) IS tracked; uploaded
# PDFs are not. Delete this line if you want them committed.
data/uploads/*.pdf
# --- Runtime app state (regenerated on run; not source) ---
data/conversations.json
data/knowledge_gaps.json
