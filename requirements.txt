fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
python-multipart>=0.0.9
pdfplumber>=0.11
sentence-transformers>=3.0
# LLM gateway (all optional — the gateway degrades to a deterministic answer
# if none are installed/configured). HF is the primary synthesis provider.
huggingface_hub>=0.24
python-dotenv>=1.0
anthropic>=0.92.0
python-docx>=1.1
pytesseract>=0.3
